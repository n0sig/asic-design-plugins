// VCS filelist for __DESIGN_NAME__
//
// List your SystemVerilog source files here, one per line.
// This filelist is read with "analyze -vcs -f", so paths are relative
// to the fc_shell invocation directory (fc/temp/). Using absolute
// paths is recommended to avoid ambiguity.
//
// Supported directives:
//   +incdir+<dir>          add an include search directory
//   +define+<MACRO>=<val>  define a preprocessor macro
//   -f <nested.f>          include another filelist
//
// Examples (absolute paths):
//   __PROJECT_PATH__/hdl/top.sv
//   __PROJECT_PATH__/hdl/sub/alu.sv
//   +incdir+__PROJECT_PATH__/hdl/include
//   +define+SYNTHESIS
//
// Examples (relative to fc/temp/):
//   ../../hdl/top.sv
//   ../../hdl/sub/alu.sv
//   +incdir+../../hdl/include

